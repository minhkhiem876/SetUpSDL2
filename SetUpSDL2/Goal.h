#ifndef GOAL.H
#define GOAL.H

#include<SDL.h> 

class Goal {
private:
	float startX, startY, width, height;
public: 
	Goal (float startX_, float startY_, float width_, float height_);
	void render 
};
#endif 